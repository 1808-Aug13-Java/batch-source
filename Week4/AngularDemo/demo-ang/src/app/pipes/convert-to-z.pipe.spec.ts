import { ConvertToZPipe } from './convert-to-z.pipe';

describe('ConvertToZPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToZPipe();
    expect(pipe).toBeTruthy();
  });
});
