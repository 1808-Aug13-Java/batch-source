//windows is out global object
window.onload = function(){
    console.log("window has loaded:)

    let x = 12;
    //console.log(x);

    int x =12;

    //cC

    myfunction(); //when we call myfunc we have accrs to the gloeb

    function2();

    console.log(x) // no longer access to x=5, global scope is
    //////////////////////////////////////////////////////////
   //////
   
   ver person;
   person = {name}
}