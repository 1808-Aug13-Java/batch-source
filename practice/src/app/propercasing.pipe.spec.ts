import { PropercasingPipe } from './propercasing.pipe';

describe('PropercasingPipe', () => {
  it('create an instance', () => {
    const pipe = new PropercasingPipe();
    expect(pipe).toBeTruthy();
  });
});
