server:
  port: 3333
spring:
  application:
    name: employee-service
eureka:
  client:
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
feign:
  hystrix:
    enabled: true